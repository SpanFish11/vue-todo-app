export const FILTERS = Object.freeze({
  ALL: 'all',
  ACTIVE: 'active',
  COMPLETED: 'completed',
})

export const FILTER_LABELS = Object.freeze({
  all: 'All',
  active: 'Active',
  completed: 'Completed',
})
